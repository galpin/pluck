#!/usr/bin/env bash
set -euo pipefail

echo "Running ruff format..."
uv run ruff format .

echo "Running ruff check with fixes..."
uv run ruff check --fix .

# Stage any files modified by ruff
git diff --name-only | xargs -r git add

echo "Running ty..."
uv run ty check src/
